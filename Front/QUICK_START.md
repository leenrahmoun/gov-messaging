# 🚀 Quick Start Guide

## خطوات البدء السريع

### 1. التأكد من تشغيل Backend

تأكد من أن الـ Backend يعمل على `http://localhost:3000`:

```bash
cd ../gov-messaging
npm start
```

### 2. إعداد Frontend

```bash
# تثبيت الحزم (إذا لم تكن مثبتة)
npm install

# إنشاء ملف .env
echo VITE_API_BASE=http://localhost:3000/api > .env
```

### 3. تشغيل التطبيق

```bash
npm run dev
```

سيعمل التطبيق على: `http://localhost:5173`

### 4. الحصول على بيانات تسجيل الدخول

#### الطريقة 1: إنشاء مستخدم Admin (الأسهل)

في مجلد الـ Backend (`gov-messaging`):

```bash
cd ../gov-messaging
npm run create-admin
```

هذا سينشئ مستخدم Admin جديد وستظهر لك:
- **Email**: (البريد الإلكتروني)
- **Password**: (كلمة المرور)

#### الطريقة 2: تسجيل مستخدم جديد

افتح Terminal جديد واختبر API:

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"user@example.com\",\"password\":\"password123\",\"name\":\"Test User\"}"
```

ثم استخدم:
- **Email**: `user@example.com`
- **Password**: `password123`

### 5. تسجيل الدخول

افتح `http://localhost:5176/login` (أو أي منفذ يظهر في Terminal) واستخدم:
- **Email**: البيانات التي حصلت عليها أعلاه
- **Password**: كلمة المرور المقابلة

## 📋 الصفحات المتاحة

- **Login** (`/login`) - صفحة تسجيل الدخول
- **Dashboard** (`/dashboard`) - لوحة التحكم الرئيسية
- **Messages** (`/messages`) - قائمة المراسلات
- **Compose** (`/compose`) - إنشاء رسالة جديدة
- **View Message** (`/messages/:id`) - عرض تفاصيل الرسالة
- **Approvals** (`/approvals`) - الموافقات (Manager/Admin فقط)
- **Users** (`/users`) - إدارة المستخدمين (Admin فقط)

## 🔧 استكشاف الأخطاء

### المشكلة: لا يمكن الاتصال بالـ API

**الحل:**
1. تأكد من أن الـ Backend يعمل على `http://localhost:3000`
2. تحقق من ملف `.env` يحتوي على `VITE_API_BASE=http://localhost:3000/api`
3. تحقق من إعدادات CORS في الـ Backend

### المشكلة: خطأ في تسجيل الدخول

**الحل:**
1. تحقق من بيانات المستخدم في قاعدة البيانات
2. افتح Developer Tools (F12) وتحقق من الأخطاء في Console
3. تحقق من أن الـ Backend يعمل بشكل صحيح

### المشكلة: الصفحات لا تظهر

**الحل:**
1. تأكد من تثبيت جميع الحزم: `npm install`
2. تحقق من عدم وجود أخطاء في Terminal
3. امسح الـ Cache: `Ctrl + Shift + R` (Windows) أو `Cmd + Shift + R` (Mac)

## 📝 ملاحظات مهمة

- يتم حفظ Token في `sessionStorage` (يُمسح عند إغلاق التبويب)
- جميع المسارات محمية ما عدا `/login`
- الصلاحيات: Admin > Manager > User
- الملفات المرفوعة يجب أن تكون أقل من 10MB

---

**جاهز للاستخدام! 🎉**

