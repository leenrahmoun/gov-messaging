# 🔧 حل مشكلة المنفذ المستخدم (Port 5173)

## المشكلة
عند تشغيل `npm run dev`، قد تظهر رسالة:
```
Port 5173 is in use, trying another one...
```

## الحلول المتاحة

### ✅ الحل 1: استخدام المنفذ الجديد (الأسهل)
التطبيق يعمل بالفعل على منفذ آخر (مثل 5174). ببساطة:
- افتح المتصفح على: `http://localhost:5174/`
- كل شيء سيعمل بشكل طبيعي

### ✅ الحل 2: إيقاف العملية التي تستخدم المنفذ 5173

#### على Windows (PowerShell):
```powershell
# البحث عن العملية
netstat -ano | findstr :5173

# إيقاف العملية (استبدل PID بالرقم الذي يظهر)
taskkill /PID <PID> /F
```

#### أو بشكل تلقائي:
```powershell
# إيقاف جميع عمليات Node.js
taskkill /F /IM node.exe
```

### ✅ الحل 3: تغيير المنفذ في vite.config.js

تم تحديث `vite.config.js` لاستخدام:
- المنفذ الافتراضي: 5173
- إذا كان مستخدماً، سيستخدم منفذ آخر تلقائياً

### ✅ الحل 4: تحديد منفذ مخصص

يمكنك تشغيل Vite على منفذ محدد:
```bash
npm run dev -- --port 3001
```

أو تعديل `package.json`:
```json
"scripts": {
  "dev": "vite --port 3001"
}
```

## ملاحظات مهمة

1. **المنفذ لا يؤثر على الاتصال بالـ Backend**
   - ملف `.env` يحتوي على: `VITE_API_BASE=http://localhost:3000/api`
   - هذا منفذ مختلف (3000) وهو خاص بالـ Backend

2. **يمكن استخدام أي منفذ للـ Frontend**
   - 5173, 5174, 3001, 8080... إلخ
   - المهم أن Backend يعمل على 3000

3. **إذا كان لديك عدة مشاريع Vite**
   - كل واحد سيستخدم منفذ مختلف تلقائياً
   - هذا طبيعي ولا مشكلة فيه

## التحقق من أن كل شيء يعمل

1. ✅ Backend يعمل على: `http://localhost:3000`
2. ✅ Frontend يعمل على: `http://localhost:5174` (أو أي منفذ آخر)
3. ✅ ملف `.env` يحتوي على: `VITE_API_BASE=http://localhost:3000/api`

**كل شيء جاهز! 🎉**

