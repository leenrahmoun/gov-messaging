# 🔐 بيانات تسجيل الدخول - Gov Messaging System

## كيفية الحصول على بيانات تسجيل الدخول

### الطريقة 1: استخدام Script إنشاء Admin (الأسهل)

في مجلد الـ Backend (`gov-messaging`):

```bash
cd ../gov-messaging
npm run create-admin
```

هذا سينشئ مستخدم Admin جديد وستظهر لك:
- **Email**: (البريد الإلكتروني)
- **Password**: (كلمة المرور)

---

### الطريقة 2: التحقق من قاعدة البيانات

إذا كان لديك مستخدم موجود بالفعل:

1. **افتح PostgreSQL:**
   ```bash
   psql -U postgres -d gov_messaging
   ```

2. **استعلام عن المستخدمين:**
   ```sql
   SELECT id, email, name, role FROM users;
   ```

3. **إعادة تعيين كلمة المرور (إذا لزم):**
   ```sql
   -- كلمة المرور الافتراضية: admin123 (مشفرة)
   UPDATE users SET password = '$2b$10$...' WHERE email = 'admin@example.com';
   ```

---

### الطريقة 3: استخدام بيانات افتراضية (إذا كانت موجودة)

تحقق من ملف `.env` في الـ Backend:
```env
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

---

### الطريقة 4: إنشاء مستخدم جديد عبر API

إذا كان لديك Backend يعمل:

```bash
# تسجيل مستخدم جديد
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "name": "Test User"
  }'
```

---

## 🔍 خطوات سريعة

### 1. تأكد من أن Backend يعمل:
```bash
cd ../gov-messaging
npm start
```

### 2. أنشئ مستخدم Admin:
```bash
npm run create-admin
```

### 3. استخدم البيانات المعروضة لتسجيل الدخول في Frontend

---

## 📝 ملاحظات مهمة

- **Email** هو معرف المستخدم (مثل: `admin@example.com`)
- **Password** هي كلمة المرور (مثل: `admin123`)
- تأكد من أن Backend يعمل على `http://localhost:3000`
- تأكد من أن قاعدة البيانات متصلة

---

## 🆘 إذا لم تعمل الطرق السابقة

1. **تحقق من قاعدة البيانات:**
   - تأكد من أن PostgreSQL يعمل
   - تأكد من أن قاعدة البيانات `gov_messaging` موجودة

2. **تحقق من Backend:**
   - افتح `http://localhost:3000` في المتصفح
   - يجب أن ترى صفحة API أو رسالة ترحيب

3. **أنشئ مستخدم جديد:**
   - استخدم `npm run create-admin` في مجلد Backend

---

**بعد الحصول على البيانات، استخدمها لتسجيل الدخول في:**
`http://localhost:5176/login` (أو أي منفذ يظهر في Terminal)

