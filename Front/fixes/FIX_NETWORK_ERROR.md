# 🔧 حل مشكلة "Network Error" في تسجيل الدخول

## المشكلة
عند محاولة تسجيل الدخول، تظهر رسالة:
```
Network Error
```

## ✅ الحلول خطوة بخطوة

### الحل 1: التحقق من ملف .env

**الخطوة 1:** تأكد من وجود ملف `.env` في مجلد `gov-messaging-ui`

**الخطوة 2:** افتح ملف `.env` وتأكد من أنه يحتوي على:
```
VITE_API_BASE=http://localhost:3000/api
```

**الخطوة 3:** إذا لم يكن موجوداً، أنشئه:
```bash
echo VITE_API_BASE=http://localhost:3000/api > .env
```

**الخطوة 4:** أعد تشغيل Frontend:
- أوقف الخادم (Ctrl+C)
- شغله من جديد: `npm run dev`

---

### الحل 2: التحقق من أن Backend يعمل

**الخطوة 1:** افتح Terminal جديد

**الخطوة 2:** اختبر Backend:
```bash
curl http://localhost:3000
```

أو افتح في المتصفح: `http://localhost:3000`

**الخطوة 3:** إذا لم يعمل، شغّل Backend:
```bash
cd ../gov-messaging
npm start
```

---

### الحل 3: التحقق من CORS في Backend

**المشكلة:** قد يكون Backend يرفض الطلبات من Frontend

**الحل:** تأكد من أن Backend يسمح بـ CORS من `http://localhost:5176`

في ملف Backend (`server.js` أو `app.js`)، يجب أن يكون:
```javascript
cors({
  origin: ['http://localhost:5173', 'http://localhost:5174', 'http://localhost:5175', 'http://localhost:5176'],
  credentials: true
})
```

---

### الحل 4: التحقق من Console المتصفح

**الخطوة 1:** افتح Developer Tools:
- اضغط `F12` في المتصفح
- أو `Ctrl + Shift + I`

**الخطوة 2:** افتح تبويب **Console**

**الخطوة 3:** حاول تسجيل الدخول مرة أخرى

**الخطوة 4:** ابحث عن الأخطاء الحمراء

**الأخطاء الشائعة:**
- `CORS policy`: مشكلة CORS
- `Failed to fetch`: Backend غير متاح
- `Network Error`: مشكلة اتصال

---

### الحل 5: التحقق من الرابط الصحيح

**تأكد من:**
- Backend يعمل على: `http://localhost:3000`
- Frontend يعمل على: `http://localhost:5176` (أو أي منفذ آخر)
- ملف `.env` يحتوي على: `VITE_API_BASE=http://localhost:3000/api`

---

### الحل 6: إعادة تشغيل كل شيء

**الخطوة 1:** أوقف Frontend (Ctrl+C)

**الخطوة 2:** أوقف Backend (Ctrl+C)

**الخطوة 3:** شغّل Backend أولاً:
```bash
cd ../gov-messaging
npm start
```

**الخطوة 4:** شغّل Frontend:
```bash
cd ../gov-messaging-ui
npm run dev
```

**الخطوة 5:** انتظر حتى يظهر:
- Backend: `Server running on port 3000`
- Frontend: `Local: http://localhost:XXXX/`

---

## 🔍 خطوات التشخيص

### 1. اختبر Backend مباشرة:

افتح Terminal واكتب:
```powershell
Invoke-RestMethod -Uri "http://localhost:3000/api/auth" -Method Get
```

إذا رأيت رد، Backend يعمل ✅

### 2. اختبر تسجيل الدخول مباشرة:

```powershell
$body = @{
    email = "admin@example.com"
    password = "admin123"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/auth/login" -Method Post -Body $body -ContentType "application/json"
```

إذا رأيت Token، Backend يعمل بشكل صحيح ✅

### 3. تحقق من Console المتصفح:

- افتح `F12`
- حاول تسجيل الدخول
- ابحث عن الأخطاء

---

## 📝 ملاحظات مهمة

1. **يجب أن يعمل Backend أولاً** قبل Frontend
2. **ملف .env مهم جداً** - بدونها Frontend لا يعرف أين Backend
3. **CORS يجب أن يكون مفعلاً** في Backend
4. **استخدم نفس المنفذ** الذي يظهر في Terminal

---

## ✅ الحل السريع (ملخص)

1. ✅ تأكد من وجود ملف `.env` مع `VITE_API_BASE=http://localhost:3000/api`
2. ✅ شغّل Backend: `cd ../gov-messaging && npm start`
3. ✅ شغّل Frontend: `npm run dev`
4. ✅ افتح Console (F12) وتحقق من الأخطاء
5. ✅ جرب تسجيل الدخول مرة أخرى

---

## 🆘 إذا استمرت المشكلة

1. **أرسل لي:**
   - رسالة الخطأ من Console (F12)
   - محتوى ملف `.env`
   - رسالة من Terminal عند تشغيل Backend

2. **جرب:**
   - استخدام Postman لاختبار Backend مباشرة
   - فتح `http://localhost:3000` في المتصفح

---

**بعد تطبيق الحلول، جرب تسجيل الدخول مرة أخرى! 🎉**

