# 🔧 حل مشكلة كلمة المرور - دليل سريع

## المشكلة
```
error: password authentication failed for user "postgres"
```

## الحل السريع (3 خطوات)

### الخطوة 1: اكتشف كلمة المرور الصحيحة

#### الطريقة أ: إذا كنت تعرف كلمة المرور
1. افتح `Command Prompt` أو `PowerShell`
2. جرب تسجيل الدخول:
   ```bash
   psql -U postgres
   ```
3. إذا طُلب منك كلمة المرور، أدخل كلمة المرور التي تعرفها
4. إذا نجحت، استخدم نفس كلمة المرور في ملف `.env`

#### الطريقة ب: إذا نسيت كلمة المرور
اتبع الخطوات في ملف `TROUBLESHOOTING.md` (الحل 2)

### الخطوة 2: عدل ملف `.env`

1. افتح ملف `.env` في المشروع
2. ابحث عن السطر:
   ```env
   DATABASE_URL=postgresql://postgres:your_password@localhost:5432/gov_messaging
   ```
3. استبدل `your_password` بكلمة المرور الصحيحة

**مثال:**
```env
DATABASE_URL=postgresql://postgres:mypassword123@localhost:5432/gov_messaging
```

### الخطوة 3: اختبر الاتصال

```bash
npm run test-db
```

إذا ظهرت رسالة `✅ الاتصال ناجح!`، فالمشكلة حُلت.

## حل بديل: استخدام مستخدم مختلف

إذا كان لديك مستخدم آخر في PostgreSQL:

1. افتح `psql`:
   ```bash
   psql -U your_username -d postgres
   ```

2. أنشئ قاعدة البيانات:
   ```sql
   CREATE DATABASE gov_messaging;
   ```

3. عدل ملف `.env`:
   ```env
   DATABASE_URL=postgresql://your_username:your_password@localhost:5432/gov_messaging
   ```

## حل للتطوير المحلي فقط (غير آمن للإنتاج)

إذا كنت في بيئة تطوير محلية فقط:

1. افتح ملف `pg_hba.conf`:
   - المسار: `C:\Program Files\PostgreSQL\14\data\pg_hba.conf`
   - (استبدل `14` برقم إصدار PostgreSQL)

2. غيّر السطر:
   ```
   # FROM
   host    all             all             127.0.0.1/32            scram-sha-256
   
   # TO
   host    all             all             127.0.0.1/32            trust
   ```

3. أعد تشغيل PostgreSQL:
   ```bash
   net stop postgresql-x64-14
   net start postgresql-x64-14
   ```

4. عدل ملف `.env` (بدون كلمة مرور):
   ```env
   DATABASE_URL=postgresql://postgres@localhost:5432/gov_messaging
   ```

⚠️ **تحذير:** هذا الحل فقط للتطوير المحلي!

## بعد إصلاح المشكلة

1. اختبر الاتصال:
   ```bash
   npm run test-db
   ```

2. قم بتهيئة قاعدة البيانات:
   ```bash
   npm run init-db
   ```

3. أنشئ مستخدم المسؤول:
   ```bash
   npm run create-admin
   ```

4. شغّل الخادم:
   ```bash
   npm start
   ```

## المساعدة الإضافية

- راجع ملف `TROUBLESHOOTING.md` للحلول التفصيلية
- راجع ملف `SETUP.md` لدليل الإعداد الكامل

---

**تم البناء بـ ❤️**

