# الخطوات التالية بعد حل المشكلة
## Next Steps After Fixing the Issue

## ✅ تم حل مشكلة الاتصال بقاعدة البيانات!

الآن يمكنك المتابعة مع الخطوات التالية:

## 📋 الخطوات المتبقية

### 1. اختبار الاتصال (اختياري لكن موصى به)

```bash
npm run test-db
```

يجب أن ترى رسالة `✅ الاتصال ناجح!`

### 2. تهيئة قاعدة البيانات

```bash
npm run init-db
```

سيتم إنشاء:
- ✅ جميع الجداول (users, messages, recipients, attachments, approvals, audit_logs)
- ✅ الفهارس (Indexes)
- ✅ المشغلات (Triggers)
- ✅ الدوال (Functions)

### 3. إنشاء مستخدم المسؤول

```bash
npm run create-admin
```

سيتم إنشاء مستخدم مسؤول بالبيانات المحددة في ملف `.env`:
- اسم المستخدم: `admin` (أو ما حددته في `.env`)
- كلمة المرور: `admin123` (أو ما حددته في `.env`)

**⚠️ مهم:** غير كلمة المرور بعد أول تسجيل دخول!

### 4. تشغيل الخادم

```bash
npm start
```

أو

```bash
npm run dev
```

سيتم تشغيل الخادم على `http://localhost:3000`

### 5. التحقق من التشغيل

افتح المتصفح وانتقل إلى:
- `http://localhost:3000` - الصفحة الرئيسية
- `http://localhost:3000/health` - فحص الحالة

يجب أن ترى:
```json
{
  "success": true,
  "status": "healthy",
  "database": "connected",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 🚀 البدء في استخدام النظام

### 1. تسجيل الدخول

استخدم Postman أو أي أداة HTTP:

```bash
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

ستحصل على Token في الاستجابة:
```json
{
  "success": true,
  "data": {
    "user": {...},
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### 2. استخدام Token في الطلبات التالية

أضف Header في جميع الطلبات:
```
Authorization: Bearer <your-token>
```

### 3. إنشاء مستخدم جديد

```bash
POST http://localhost:3000/api/auth/register
Content-Type: application/json

{
  "username": "user1",
  "email": "user1@example.com",
  "password": "password123",
  "full_name": "مستخدم تجريبي",
  "role": "user",
  "department": "قسم تجريبي"
}
```

### 4. إنشاء مراسلة

```bash
POST http://localhost:3000/api/messages
Authorization: Bearer <your-token>
Content-Type: application/json

{
  "subject": "اختبار المراسلة",
  "content": "هذا محتوى المراسلة",
  "message_type": "internal",
  "priority": "normal",
  "requires_approval": false,
  "recipient_ids": [2]
}
```

## 📚 المراجع المفيدة

- `README.md` - التوثيق الكامل
- `SETUP.md` - دليل الإعداد
- `TROUBLESHOOTING.md` - حل المشاكل
- `FIX_PASSWORD.md` - حل مشكلة كلمة المرور

## 🎯 ما يمكنك فعله الآن

1. ✅ **استكشف APIs** - راجع جميع Endpoints في `README.md`
2. ✅ **أنشئ مستخدمين** - استخدم API `/api/users`
3. ✅ **أنشئ مراسلات** - استخدم API `/api/messages`
4. ✅ **رفع مرفقات** - استخدم API `/api/attachments`
5. ✅ **نظام الموافقات** - استخدم API `/api/approvals`
6. ✅ **مراجعة Audit Logs** - استخدم API `/api/audit`

## 🔒 نصائح الأمان

1. **غير JWT_SECRET** في ملف `.env` قبل الإنتاج
2. **غير كلمة مرور المسؤول** بعد أول تسجيل دخول
3. **راجع Audit Logs** بانتظام
4. **احفظ ملف `.env` آمنًا** ولا ترفعه إلى Git

## 🐛 إذا واجهت مشاكل

1. راجع ملف `TROUBLESHOOTING.md`
2. جرب `npm run test-db` للتحقق من الاتصال
3. راجع سجلات الخادم في Terminal

## 🎉 تهانينا!

النظام جاهز الآن للاستخدام! يمكنك البدء في:
- إنشاء المستخدمين
- إدارة المراسلات
- رفع المرفقات
- نظام الموافقات
- مراجعة Audit Logs

---

**تم البناء بـ ❤️ - استمتع باستخدام النظام!**

