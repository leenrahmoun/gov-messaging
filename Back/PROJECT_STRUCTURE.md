# 📁 هيكل المشروع الكامل
## Complete Project Structure

## 🎯 نظرة عامة

هذا المشروع هو **نظام إدارة مراسلات حكومية رقمية** كامل مبني باستخدام:
- **Backend:** Node.js + Express
- **Database:** PostgreSQL
- **Authentication:** JWT
- **File Upload:** Multer
- **Security:** Helmet, CORS, bcrypt

---

## 📂 هيكل المجلدات والملفات

```
gov-messaging/
│
├── 📁 controllers/          # Controllers (منطق العمل)
├── 📁 routes/               # Routes (مسارات API)
├── 📁 middleware/           # Middleware (الطبقات الوسطية)
├── 📁 database/             # قاعدة البيانات
├── 📁 db/                   # اتصال قاعدة البيانات
├── 📁 scripts/              # Scripts مساعدة
├── 📁 public/               # ملفات ثابتة (صفحة الاختبار)
├── 📁 uploads/              # الملفات المرفوعة (يُنشأ تلقائياً)
├── 📁 node_modules/         # الحزم المثبتة
│
├── 📄 server.js             # نقطة البداية الرئيسية
├── 📄 package.json          # إعدادات المشروع والحزم
├── 📄 package-lock.json     # قفل إصدارات الحزم
├── 📄 .env                  # متغيرات البيئة (لا يُرفع إلى Git)
├── 📄 .gitignore            # ملفات يتم تجاهلها في Git
├── 📄 env.example           # مثال لملف البيئة
│
└── 📄 README.md             # التوثيق الرئيسي
    📄 SETUP.md              # دليل الإعداد
    📄 NEXT_STEPS.md         # الخطوات التالية
    📄 HOW_TO_TEST.md        # كيفية اختبار API
    📄 QUICK_START.md        # البدء السريع
    📄 TROUBLESHOOTING.md    # حل المشاكل
    📄 FIX_PASSWORD.md       # حل مشكلة كلمة المرور
    📄 PROJECT_STRUCTURE.md  # هذا الملف
```

---

## 📁 المجلدات والملفات بالتفصيل

### 🎮 1. controllers/ - Controllers (منطق العمل)

هذا المجلد يحتوي على جميع Controllers التي تحتوي على منطق العمل الرئيسي.

#### 📄 authController.js
**الوظيفة:** إدارة المصادقة والتسجيل
**الدوال:**
- `register()` - تسجيل مستخدم جديد
- `login()` - تسجيل الدخول وإنشاء JWT Token
- `getProfile()` - الحصول على معلومات المستخدم الحالي
- `updateProfile()` - تحديث معلومات المستخدم
- `changePassword()` - تغيير كلمة المرور

**الحالة:** ✅ مكتمل

#### 📄 userController.js
**الوظيفة:** إدارة المستخدمين (CRUD)
**الدوال:**
- `getUsers()` - الحصول على قائمة المستخدمين (مع pagination)
- `getUserById()` - الحصول على مستخدم محدد
- `createUser()` - إنشاء مستخدم جديد (Admin فقط)
- `updateUser()` - تحديث مستخدم (Admin فقط)
- `deleteUser()` - حذف مستخدم (Admin فقط)
- `resetPassword()` - إعادة تعيين كلمة مرور (Admin فقط)

**الحالة:** ✅ مكتمل

#### 📄 messageController.js
**الوظيفة:** إدارة المراسلات
**الدوال:**
- `getMessages()` - الحصول على قائمة المراسلات (مع filters و pagination)
- `getMessageById()` - الحصول على مراسلة محددة (مع المستلمين والمرفقات)
- `createMessage()` - إنشاء مراسلة جديدة
- `updateMessage()` - تحديث مراسلة
- `deleteMessage()` - حذف مراسلة
- `sendMessage()` - إرسال مراسلة

**الحالة:** ✅ مكتمل

#### 📄 attachmentController.js
**الوظيفة:** إدارة المرفقات
**الدوال:**
- `getAttachments()` - الحصول على قائمة المرفقات لمراسلة
- `downloadAttachment()` - تحميل مرفق
- `deleteAttachment()` - حذف مرفق

**ملاحظة:** رفع الملفات يتم في `routes/attachmentRoutes.js`

**الحالة:** ✅ مكتمل

#### 📄 approvalController.js
**الوظيفة:** إدارة الموافقات
**الدوال:**
- `getApprovals()` - الحصول على قائمة الموافقات
- `getApprovalById()` - الحصول على موافقة محددة
- `approveMessage()` - الموافقة على مراسلة
- `rejectMessage()` - رفض مراسلة

**الحالة:** ✅ مكتمل

#### 📄 auditController.js
**الوظيفة:** إدارة سجل الأحداث (Audit Log)
**الدوال:**
- `getAuditLogs()` - الحصول على سجل الأحداث (مع filters)
- `getAuditStats()` - الحصول على إحصائيات سجل الأحداث

**الحالة:** ✅ مكتمل

---

### 🛣️ 2. routes/ - Routes (مسارات API)

هذا المجلد يحتوي على جميع Routes التي تحدد endpoints للـ API.

#### 📄 authRoutes.js
**المسارات:**
- `GET /api/auth` - معلومات عن Auth API
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/profile` - الملف الشخصي (محمي)
- `PUT /api/auth/profile` - تحديث الملف الشخصي (محمي)
- `POST /api/auth/change-password` - تغيير كلمة المرور (محمي)

**الحالة:** ✅ مكتمل

#### 📄 userRoutes.js
**المسارات:**
- `GET /api/users` - قائمة المستخدمين (محمي)
- `GET /api/users/:id` - مستخدم محدد (محمي)
- `POST /api/users` - إنشاء مستخدم (Admin فقط)
- `PUT /api/users/:id` - تحديث مستخدم (Admin فقط)
- `DELETE /api/users/:id` - حذف مستخدم (Admin فقط)
- `POST /api/users/:id/reset-password` - إعادة تعيين كلمة مرور (Admin فقط)

**الحالة:** ✅ مكتمل

#### 📄 messageRoutes.js
**المسارات:**
- `GET /api/messages` - قائمة المراسلات (محمي)
- `GET /api/messages/:id` - مراسلة محددة (محمي)
- `POST /api/messages` - إنشاء مراسلة (محمي)
- `PUT /api/messages/:id` - تحديث مراسلة (محمي)
- `DELETE /api/messages/:id` - حذف مراسلة (محمي)
- `POST /api/messages/:id/send` - إرسال مراسلة (محمي)

**الحالة:** ✅ مكتمل

#### 📄 attachmentRoutes.js
**المسارات:**
- `POST /api/attachments/:messageId/upload` - رفع مرفق (محمي)
- `GET /api/attachments/:messageId` - قائمة المرفقات (محمي)
- `GET /api/attachments/download/:id` - تحميل مرفق (محمي)
- `DELETE /api/attachments/:id` - حذف مرفق (محمي)

**الحالة:** ✅ مكتمل

#### 📄 approvalRoutes.js
**المسارات:**
- `GET /api/approvals` - قائمة الموافقات (محمي)
- `GET /api/approvals/:id` - موافقة محددة (محمي)
- `POST /api/approvals/:id/approve` - الموافقة على مراسلة (Manager/Admin فقط)
- `POST /api/approvals/:id/reject` - رفض مراسلة (Manager/Admin فقط)

**الحالة:** ✅ مكتمل

#### 📄 auditRoutes.js
**المسارات:**
- `GET /api/audit` - سجل الأحداث (Manager/Admin فقط)
- `GET /api/audit/stats` - إحصائيات سجل الأحداث (Manager/Admin فقط)

**الحالة:** ✅ مكتمل

---

### 🔒 3. middleware/ - Middleware (الطبقات الوسطية)

هذا المجلد يحتوي على جميع Middleware functions.

#### 📄 auth.js
**الوظيفة:** المصادقة والصلاحيات
**الدوال:**
- `authenticateToken()` - التحقق من JWT Token
- `requireAdmin()` - التحقق من صلاحيات Admin
- `requireManager()` - التحقق من صلاحيات Manager أو Admin

**الحالة:** ✅ مكتمل

#### 📄 audit.js
**الوظيفة:** تسجيل الأحداث في Audit Log
**الدوال:**
- `auditLog()` - Middleware لتسجيل جميع الأحداث
- `getEntityType()` - تحديد نوع الكيان
- `getEntityId()` - الحصول على معرف الكيان
- `generateDescription()` - إنشاء وصف للحدث

**الحالة:** ✅ مكتمل

#### 📄 upload.js
**الوظيفة:** رفع الملفات باستخدام Multer
**الإعدادات:**
- المجلد الوجهة: `./uploads`
- الحد الأقصى لحجم الملف: 10MB
- أنواع الملفات المسموحة: PDF, Word, Excel, Images, Text

**الحالة:** ✅ مكتمل

---

### 💾 4. database/ - قاعدة البيانات

هذا المجلد يحتوي على ملفات قاعدة البيانات.

#### 📄 schema.sql
**الوظيفة:** تعريف قاعدة البيانات
**المحتوى:**
- جدول `users` - المستخدمين
- جدول `messages` - المراسلات
- جدول `recipients` - المستلمين
- جدول `attachments` - المرفقات
- جدول `approvals` - الموافقات
- جدول `audit_logs` - سجل الأحداث
- الفهارس (Indexes)
- المشغلات (Triggers)
- الدوال (Functions)

**الحالة:** ✅ مكتمل

#### 📄 init.js
**الوظيفة:** تهيئة قاعدة البيانات
**الوظيفة:** قراءة `schema.sql` وإنشاء جميع الجداول

**الحالة:** ✅ مكتمل

---

### 🔌 5. db/ - اتصال قاعدة البيانات

هذا المجلد يحتوي على ملفات اتصال قاعدة البيانات.

#### 📄 index.js
**الوظيفة:** إدارة اتصال PostgreSQL
**المحتوى:**
- إنشاء Connection Pool
- دوال `query()` و `getClient()`
- معالجة الأخطاء

**الحالة:** ✅ مكتمل

---

### 🛠️ 6. scripts/ - Scripts مساعدة

هذا المجلد يحتوي على scripts مساعدة.

#### 📄 createAdmin.js
**الوظيفة:** إنشاء مستخدم المسؤول الأول
**الاستخدام:**
```bash
npm run create-admin
```

**الحالة:** ✅ مكتمل

#### 📄 testConnection.js
**الوظيفة:** اختبار الاتصال بقاعدة البيانات
**الاستخدام:**
```bash
npm run test-db
```

**الحالة:** ✅ مكتمل

---

### 🌐 7. public/ - ملفات ثابتة

هذا المجلد يحتوي على الملفات الثابتة (صفحة الاختبار).

#### 📄 index.html
**الوظيفة:** صفحة اختبار API
**المحتوى:**
- واجهة مستخدم لاختبار API
- تسجيل الدخول
- عرض النتائج
- حفظ Token تلقائياً

**الحالة:** ✅ مكتمل

---

### 📦 8. الملفات الرئيسية

#### 📄 server.js
**الوظيفة:** نقطة البداية الرئيسية
**المحتوى:**
- تهيئة Express
- إعداد Middleware
- ربط Routes
- معالجة الأخطاء
- تشغيل الخادم

**الحالة:** ✅ مكتمل

#### 📄 package.json
**الوظيفة:** إعدادات المشروع
**المحتوى:**
- اسم المشروع وإصداره
- الحزم المثبتة
- Scripts (start, dev, init-db, create-admin, test-db)
- معلومات المشروع

**الحالة:** ✅ مكتمل

#### 📄 .env
**الوظيفة:** متغيرات البيئة
**المحتوى:**
- `DATABASE_URL` - رابط قاعدة البيانات
- `PORT` - منفذ الخادم
- `JWT_SECRET` - مفتاح JWT
- `UPLOAD_DIR` - مجلد المرفقات
- إعدادات المسؤول

**الحالة:** ⚠️ يجب إنشاؤه من `env.example`

#### 📄 .gitignore
**الوظيفة:** ملفات يتم تجاهلها في Git
**المحتوى:**
- `node_modules/`
- `.env`
- `uploads/`
- ملفات النظام

**الحالة:** ✅ مكتمل

---

## 📊 إحصائيات المشروع

### الملفات المكتملة
- ✅ **6 Controllers** - جميع Controllers مكتملة
- ✅ **6 Routes** - جميع Routes مكتملة
- ✅ **3 Middleware** - جميع Middleware مكتملة
- ✅ **1 Database Schema** - مكتمل
- ✅ **2 Scripts** - مكتملة
- ✅ **1 صفحة اختبار** - مكتملة
- ✅ **1 Server** - مكتمل

### الوظائف المكتملة
- ✅ المصادقة والتسجيل (JWT)
- ✅ إدارة المستخدمين (CRUD)
- ✅ إدارة المراسلات (CRUD + Send)
- ✅ رفع الملفات (Multer)
- ✅ نظام الموافقات
- ✅ Audit Logging
- ✅ نظام الصلاحيات (RBAC)
- ✅ حماية API (Helmet, CORS)

### الجداول في قاعدة البيانات
- ✅ `users` - المستخدمين
- ✅ `messages` - المراسلات
- ✅ `recipients` - المستلمين
- ✅ `attachments` - المرفقات
- ✅ `approvals` - الموافقات
- ✅ `audit_logs` - سجل الأحداث

---

## 🎯 حالة المشروع

### ✅ المكتمل (100%)

1. **Backend Infrastructure**
   - ✅ Express Server
   - ✅ PostgreSQL Connection
   - ✅ Middleware Setup
   - ✅ Error Handling

2. **Authentication & Authorization**
   - ✅ JWT Authentication
   - ✅ Password Hashing (bcrypt)
   - ✅ Role-based Access Control
   - ✅ User Registration & Login

3. **User Management**
   - ✅ CRUD Operations
   - ✅ User Roles (Admin, Manager, User)
   - ✅ Password Reset
   - ✅ Profile Management

4. **Message Management**
   - ✅ CRUD Operations
   - ✅ Message Types (Internal, External, Official)
   - ✅ Message Priorities
   - ✅ Message Status
   - ✅ Send Messages
   - ✅ Recipients Management

5. **File Management**
   - ✅ File Upload (Multer)
   - ✅ File Download
   - ✅ File Delete
   - ✅ File Type Validation
   - ✅ File Size Validation

6. **Approval System**
   - ✅ Approval Requests
   - ✅ Approve/Reject Messages
   - ✅ Approval Comments
   - ✅ Approval History

7. **Audit Logging**
   - ✅ Event Logging
   - ✅ Audit Log Queries
   - ✅ Audit Statistics
   - ✅ User Activity Tracking

8. **Security**
   - ✅ Helmet.js
   - ✅ CORS
   - ✅ Input Validation
   - ✅ SQL Injection Protection
   - ✅ XSS Protection

9. **Documentation**
   - ✅ README.md
   - ✅ SETUP.md
   - ✅ API Documentation
   - ✅ Troubleshooting Guides

10. **Testing Tools**
    - ✅ Test Page (HTML)
    - ✅ Database Test Script
    - ✅ Admin Creation Script

### 🚧 قيد التطوير (0%)

- لا يوجد حالياً

### 📋 المخطط له (Future)

1. **Frontend Application**
   - [ ] React/Vue.js Frontend
   - [ ] User Dashboard
   - [ ] Message Composer
   - [ ] File Upload Interface

2. **Additional Features**
   - [ ] Email Notifications
   - [ ] Real-time Notifications (WebSocket)
   - [ ] Advanced Search
   - [ ] Reports & Analytics
   - [ ] Data Export
   - [ ] Backup System

3. **Testing**
   - [ ] Unit Tests
   - [ ] Integration Tests
   - [ ] E2E Tests

4. **Deployment**
   - [ ] Docker Configuration
   - [ ] CI/CD Pipeline
   - [ ] Production Setup

---

## 🎓 كيفية استخدام المشروع

### 1. الإعداد الأولي
```bash
# تثبيت الحزم
npm install

# إعداد ملف .env
copy env.example .env

# تهيئة قاعدة البيانات
npm run init-db

# إنشاء مستخدم المسؤول
npm run create-admin
```

### 2. التشغيل
```bash
# تشغيل الخادم
npm start

# أو
npm run dev
```

### 3. الاختبار
```bash
# فتح صفحة الاختبار
http://localhost:3000

# أو استخدام Postman
# POST http://localhost:3000/api/auth/login
```

---

## 📚 المراجع

- **README.md** - التوثيق الرئيسي
- **SETUP.md** - دليل الإعداد
- **HOW_TO_TEST.md** - كيفية اختبار API
- **TROUBLESHOOTING.md** - حل المشاكل

---

## 🎉 الخلاصة

المشروع **مكتمل 100%** وجاهز للاستخدام! جميع الوظائف الأساسية تم تنفيذها:
- ✅ Backend API كامل
- ✅ قاعدة بيانات منظمة
- ✅ نظام أمان متقدم
- ✅ توثيق شامل
- ✅ أدوات اختبار

يمكنك الآن:
1. تشغيل المشروع
2. اختبار جميع APIs
3. البدء في استخدام النظام
4. إضافة ميزات جديدة حسب الحاجة

---

**تم البناء بـ ❤️ - استمتع باستخدام النظام!**

